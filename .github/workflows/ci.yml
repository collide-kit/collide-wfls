name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  format:
    uses: ./.github/workflows/format.yml
    secrets: inherit

  test:
    uses: ./.github/workflows/test.yml
    secrets: inherit

  test-cache-deps:
    uses: ./.github/workflows/test-cache-deps.yml
    secrets: inherit

  ci-ok:
    name: CI OK
    runs-on: ubuntu-latest
    needs: [format, test, test-cache-deps]
    steps:
      - run: echo "🟢 All required jobs finished successfully"
